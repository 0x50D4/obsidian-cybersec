We can make a simple function that returns a value:
```c
int f() {
	return 123;
};
```

Let's compile it again in different architectures:
[[x86 returning]]
[[ARM returning]]
[[MIPS returning]]

#reversing