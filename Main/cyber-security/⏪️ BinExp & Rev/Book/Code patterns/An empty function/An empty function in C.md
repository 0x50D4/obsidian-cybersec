The simplest function we can write is one that does nothing, here is it in C:

```c
void f() {
	return;
};
```

Let's compile it different ways:

[[empty x86]]
[[empty ARM]]
[[empty MIPS]]


#reversing